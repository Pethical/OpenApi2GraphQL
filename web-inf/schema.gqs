type Query{
  GetUsers: [User]
  GetCats: [Cat]
  GetCat(id: Int): Cat
  GetUser(id: Int): User
}

type User {
  id: Int,
  name : String,
  regDate: String,
  cats:[Cat]
}

type Cat{
  id: Int,
  name:String,
  live:Boolean
}